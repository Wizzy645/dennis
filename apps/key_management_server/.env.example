JWT_SHARED_SECRET=dev_secret_key_hr7382yhf9283yhf
JWT_ISSUER=local-emr-auth-service

# Standalone Key Management Server database (stores tenant master keys + audit events).
#
# Uses the local PostgreSQL server. The server initializes its tables on startup.
# Example:
# KEY_SERVER_DB_DSN=postgresql://emr_db_admin:TestEnv_DB_Auth_2026!@127.0.0.1:5432/postgres?sslmode=prefer
KEY_SERVER_DB_DSN=postgresql://emr_db_admin:TestEnv_DB_Auth_2026!@127.0.0.1:5432/postgres?sslmode=prefer

